import { Layout, Layer } from './components'

export const App = () => (
  <Layout>
    <Layer thumbnail={'rober'} />
    <Layer thumbnail={'rober'} />
    <Layer />
  </Layout>
)

export default App
